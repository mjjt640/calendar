<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@android:drawable/sym_def_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Calendar">

        <activity
            android:name=".ui.schedule.AddScheduleActivity"
            android:exported="false"
            android:theme="@style/Theme.Calendar" />

        <activity
            android:name=".ui.schedule.RecurrenceConfigActivity"
            android:exported="false"
            android:theme="@style/Theme.Calendar" />

        <activity
            android:name=".ui.settings.ReminderSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.Calendar" />

        <activity
            android:name=".ui.settings.DataManagementActivity"
            android:exported="false"
            android:theme="@style/Theme.Calendar" />

        <activity
            android:name=".ui.profile.ProfileSetupActivity"
            android:exported="false"
            android:theme="@style/Theme.Calendar" />

        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.Calendar" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Calendar" />

        <activity
            android:name=".ui.launcher.LauncherActivity"
            android:exported="true"
            android:theme="@style/Theme.Calendar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
